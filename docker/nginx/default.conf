# Nginx configuration
server {
    listen 80;
    server_name chat.loc;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/html/frontend;

    set $virtualdir "";
    set $realdir "";

    if ($request_uri ~ ^/([^/]*)/.*$ ) {
        set $virtualdir /$1;
    }

    if (-d "$document_root$virtualdir") {
        set $realdir "${virtualdir}";
    }

    location / {
        try_files $uri $uri/ $realdir/index.php?$args;
    }
    location ~ \.php$ {
        #try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

server {
    listen 80;
    server_name users.api.loc;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/html/user_api;

    set $virtualdir "";
    set $realdir "";

    if ($request_uri ~ ^/([^/]*)/.*$ ) {
        set $virtualdir /$1;
    }

    if (-d "$document_root$virtualdir") {
        set $realdir "${virtualdir}";
    }

    location / {
        try_files $uri $uri/ $realdir/index.php?$args;
    }
    location ~ \.php$ {
        #try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

}

server {
    listen 80;
    server_name chat.api.loc;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/html/chat_api;

    set $virtualdir "";
    set $realdir "";

    if ($request_uri ~ ^/([^/]*)/.*$ ) {
        set $virtualdir /$1;
    }

    if (-d "$document_root$virtualdir") {
        set $realdir "${virtualdir}";
    }

    location / {
        try_files $uri $uri/ $realdir/index.php?$args;
    }
    location ~ \.php$ {
        #try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

#server {
#    listen 80;
#    server_name task3.loc;#
#
 #   #index index.php index.html index-temp.html;
  #  error_log  /var/log/nginx/error.log;
   # access_log /var/log/nginx/access.log;
    #root /var/www/html/frontend;

#    location / {
#        index index.php
#        try_files $uri /index.php;
#    }

    #location ^~ /authorise {
    #    #index authorise-temp.html
    #    try_files $uri /templates/authorise-temp.html;
    #}

    #location ^~ /register {
    #    #index register-temp.html
    #    try_files $uri /templates/register-temp.html;
    #}

    #location ^~ /main.css {
    #    index main.css;
    #    root /var/www/html/frontend/css/;
    #    #try_files $uri index-temp.html;
    #}

    #location ~ /chat_api/ {
    #    #fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #    #fastcgi_pass php:9000;
    #    fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        #fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
    #location ~ /user_api/ {
        #root /var/www/html/user_api/web/public/index.php;
        #set $realdir "/web/public";
        #fastcgi_split_path_info ^(.+\.php)(/.+)$;
        #fastcgi_pass php:9000;
        #fastcgi_index index.php;
    #    include fastcgi_params;
    #    fastcgi_param SCRIPT_FILENAME /var/www/html/user_api/web/public/index.php;
        #fastcgi_param PATH_INFO $fastcgi_path_info;
    #}
    #location ~ /user_api/style  {
    #        root /var/www/html/frontend/css/main.css;

    #        #fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #        fastcgi_pass php:9000;
    #        #fastcgi_index main.css;
    #        include fastcgi_params;
    #        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #        #fastcgi_param PATH_INFO $fastcgi_path_info;
    #    }
#}

# server {
#     server_name localhost;

#     listen 443 ssl;
#     fastcgi_param HTTPS on;

#     ssl_certificate /etc/ssl/server.pem;
#     ssl_certificate_key /etc/ssl/server.key;
#     ssl_protocols SSLv3 TLSv1 TLSv1.1 TLSv1.2;

#     index index.php index.html;
#     error_log  /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;
#     root /var/www/html/public;

#     set $virtualdir "";
#     set $realdir "";

#     if ($request_uri ~ ^/([^/]*)/.*$ ) {
#         set $virtualdir /$1;
#     }

#     if (-d "$document_root$virtualdir") {
#         set $realdir "${virtualdir}";
#     }

#     location / {
#         try_files $uri $uri/ $realdir/index.php?$args;
#     }

#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass php:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }
